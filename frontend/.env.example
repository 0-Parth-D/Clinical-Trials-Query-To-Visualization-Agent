# Optional: call the FastAPI backend directly (requires CORS; dev origins are allowed in app/main.py).
# Leave unset to use the Vite dev proxy instead (POST to /api/v1/query → http://127.0.0.1:8000/v1/query).
# VITE_API_BASE_URL=http://127.0.0.1:8000
