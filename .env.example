# Optional: enables LLM-based intent (OpenAI-compatible).
OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# ClinicalTrials.gov fetch caps (optional).
# CTGOV_MAX_STUDIES=1000
# CTGOV_PAGE_SIZE=100

# Citations: excerpts per datum / trials tracked per aggregate bucket.
# CITATION_EXCERPTS_PER_DATUM=3
# CITATION_TRIALS_PER_BUCKET_CAP=50
